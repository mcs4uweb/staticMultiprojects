# docker-compose.yml
services:
  focalboard:
    image: mattermost/focalboard:latest
    container_name: focalboard
    ports:
      - "8080:8000"
    volumes:
      - ./focalboard-data:/opt/focalboard/data
      - ./focalboard-config:/opt/focalboard/config
    restart: unless-stopped